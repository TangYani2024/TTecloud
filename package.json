{
  "name": "ttecloud",
  "version": "1.0.0",
  "description": "基于 Cloudflare Pages Functions + D1 + S3 兼容对象存储的现代化极速云盘与图床系统",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --d1=DB",
    "deploy": "wrangler pages deploy public --project-name=TTecloud"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
